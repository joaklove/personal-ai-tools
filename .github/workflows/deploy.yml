# 这里的名字可以随便起
name: Deploy Vite App

on:
  # 监听 main 分支的 push 事件
  push:
    branches: ["main" ]
  # 允许你手动点击按钮触发部署
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      - name: Setup Node.js ⚙️
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install Dependencies 🔧
        run: npm install

      - name: Build App 🏗️
        run: npm run build
        # 注意：Vite 默认打包到 dist 文件夹，如果你的配置不同，这里可能需要改
        
      - name: Upload Artifact 📤
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist # 👈 这里非常重要，告诉 GitHub 只上传打包好的 dist 文件夹

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to GitHub Pages 🚀
        id: deployment
        uses: actions/deploy-pages@v4